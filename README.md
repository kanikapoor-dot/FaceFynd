# FaceFynd
 Find every face in your local photo library. Private. Fast. Local.

Setup: > Download the YuNet ONNX model from [OpenCV Zoo] and place it in the /models directory before running the application.